export type WorkspaceSettings = {
  name: string;
  domain: string;
};

export type ProfileSettings = {
  firstName: string;
  lastName: string;
  email: string;
  avatarUrl?: string;
  bio?: string;
};
